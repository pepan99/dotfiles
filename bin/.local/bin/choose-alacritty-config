#!/usr/bin/env bash

if xrandr | grep -w "connected" | grep "DP-2"; then
    alacritty --config-file ~/.config/alacritty/alacritty.yml
else
    alacritty --config-file ~/.config/alacritty/alacritty-no-monitor.yml
fi

